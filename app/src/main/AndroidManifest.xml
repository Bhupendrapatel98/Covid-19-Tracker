<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.covid_19tracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.DisttDetailActivity"></activity>
        <activity android:name=".ui.SplashActivity" />
        <activity android:name=".ui.DisttListActivity" />
        <activity android:name=".ui.StateDataActivity" />
        <activity android:name=".ui.StateListActivity" />
        <activity android:name=".ui.CountryDetailActivity" />
        <activity android:name=".ui.AffectedCountriesActivity" />
        <activity android:name=".ui.AffectedConActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>